import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#4F63AC',
    borderRadius: 8,
    paddingHorizontal: 8,
    paddingVertical: 20,
    width: '100%',
  },
  innerText: {
    color: 'white',
    fontSize: 16,
    textAlign: 'center',
  },
});
